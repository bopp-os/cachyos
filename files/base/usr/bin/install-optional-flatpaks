#!/bin/bash
# Compatibility alias — the canonical name is guara-install-flatpaks.
# This wrapper exists so any existing references to install-optional-flatpaks
# continue to work.
exec /usr/bin/guara-install-flatpaks "$@"
