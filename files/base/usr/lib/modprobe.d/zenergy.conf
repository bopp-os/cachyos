# zenergy has no hardware alias to autoload from; k10temp autoloads on every AMD Zen system,
# so chain zenergy after it instead of using modules-load.d (which would fail on Intel).
softdep k10temp post: zenergy
